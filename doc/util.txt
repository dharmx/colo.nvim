M.tbl.rm_bool({options})                                         *M.tbl.rm_bool*
    Remove boolean keys from a table.

    Parameters: ~
        {options}  (table<boolean|any,any>)

    Returns: ~
        (table)


M.tbl.sum()                                                          *M.tbl.sum*

    Returns: ~
        (number)


M.tbl.transform_options({options})                     *M.tbl.transform_options*
    Helper for transforming indexed decoration options into key-value pairs.

    Parameters: ~
        {options}  (table<number|string,string|number|boolean>)

    Returns: ~
        (table<number|string,string|number|boolean>)


M.str.capitalize({item})                                      *M.str.capitalize*
    Uppercases the initial charater of words.

    Parameters: ~
        {item}  (string)

    Returns: ~
        (string)


M.str.trim_left({item})                                        *M.str.trim_left*
    Remove all leading whitespace characters.

    Parameters: ~
        {item}  (string)

    Returns: ~
        (string)


M.str.trim_right({item})                                      *M.str.trim_right*
    Remove all trailing whitespace characters.

    Parameters: ~
        {item}  (string)

    Returns: ~
        (string)


M.plugin.path()                                                  *M.plugin.path*
    Returns the path where the plugin directory is at.

    Returns: ~
        (string)


M.plugin.scan({fragment}, {options})                             *M.plugin.scan*
    Scan for both the plugin directory and the user configuration directory.

    Parameters: ~
        {fragment}  (string)
        {options}   (table<string,boolean|function>)    options.user      (boolean): add only user paths.
                                                        options.on_insert (fun(path: string) -> string): callback that may change the path when detected.
                                                      @return table<string>


M.canned.to_module_path({path})                        *M.canned.to_module_path*
    Canned function to remove the /../../plugin_path/lua part from /../../plugin_path/lua/colo/groups/base/prompts.lua

    Parameters: ~
        {path}  (string)

    Returns: ~
        (string)


M.canned.filenamermx({path})                              *M.canned.filenamermx*
    Remove just the extension from the path. See |filename-modifiers| and |fnamemodify()|.

    Parameters: ~
        {path}  (string)

    Returns: ~
        (string)


vim:tw=78:ts=8:noet:ft=help:norl:

